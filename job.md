[개요]
LLM 챗봇으로 업무(정보 수집, 팩트 체크, 구조 설계 등)를 하다보면 단일 LLM 모델의 특징에 따라 편향된 결과를 받게 되는 경우가 있음. 이를 검증하는 것은 여전히 인간의 몫이고 빠른 자료 생성에 인간의 검증 업무는 그 만큼 늘어나고 있음.

[목표]
진행 위임자, 토론자A, 토론자B 등의 역할을 전역으로 설정하고 한가지 주제에 대해서 질문을 던지면 진행자는 결과 도출을 하기 위한 토론 정책을 수립하여 토론자A와 토론자B에게 질문 답변 및 결과 도출의 역할을 시킴

[구현 조건]
- 진행 위임자, 토론자들은 각기 다른 LLM API로 설정 할 수 있어야 함 (편향 방지)
- 무한 재귀나 과도한 토큰 소비는 진행 위임자가 조정할 수 있도록 사용자가 설정하거나 하네스를 만들어내야 함
- 토론 주제A와 토론 주제B는 서로 컨텍스트가 공유되면 안되며 서로 격리된 메모리로 컨텐츠가 혼재되어선 안됨
- 토론에 걸리는 시간이 너무 오래 걸리지 않도록 최대 진행 시간을 지정할 수 있어야 함

[서비스 방식]
- 웹으로 구현해서 설치 없이 편하게 이용할 수 있어야 함
- 패스키 인증 및 개인과 설정으로 AI API 키등 본인이 이용 중인 서비스를 등록해서 사용
- 결과물은 md나 docx 파일로 다운받을 수 있어야 함

[구현 기술]
- 파이썬이나 typescript/javascript 사용
- MariaDB 또는 PostgreSQL 등의 RDB 스토리지 사용
- Docker Compose로 빌드 및 배포를 간소화 할 수 있어야 함
- 계정 별 개인화 기능이 잘 나뉘어져야 함
- 프로젝트 파일 구성은 무조건 한 파일에 백엔드, 프론트엔드 코드를 몰아넣지 말고 기능과 도메인 별로 패키지 디렉토리를 구성해서 사람이 잃을 수 있기 쉽도록 해야함
- 코드가 너무 장황하거나 유사 로직에 대해 매번 새로운 코드를 생성하여서는 안됨
- 인간이 함께 디버깅 할 수 있도록 코드의 가독성을 중심으로 코드 생성 해야 함
- 라이브는 클라우드 서버에 배포될 것이고, Cloudflare(도메인, CDN)와 Hetzner(서버 인스턴스) 를 이용할 것임
